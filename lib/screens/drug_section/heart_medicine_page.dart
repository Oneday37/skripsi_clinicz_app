import 'package:flutter/material.dart';

class HeartMedicinePage extends StatelessWidget {
  const HeartMedicinePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Ini adalah Halaman Khusus Untuk Obat Penyakit Jantung"),
      ),
    );
  }
}
